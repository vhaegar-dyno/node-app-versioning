services:
  # Backend Application
  app:
    build: .
    container_name: holmium-boilerplate---v1.0.0
    restart: always
    env_file:
      - .env.development
    ports:
      - '5000:5000'
    volumes:
      - .:/app
      - /app/node_modules
    command: ['npm', 'run', 'start:dev']


